#!/bin/bash

set -x 
id
ls -la 

if [ -x ${STI_SCRIPTS_PATH}/nss-wrapper-setup ]; then
    source ${STI_SCRIPTS_PATH}/nss-wrapper-setup
fi

if [ -x  ${STI_SCRIPTS_PATH}/run ]; then
    source ${STI_SCRIPTS_PATH}/run
fi

